apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: ${{ values.name }}
  title: ${{ values.displayName }}
  description: "Karpathy research agent — topic: ${{ values.topic | replace('\n', ' ') | truncate(80, true) }}"
  links:
    - url: "https://devspaces.apps.salamander.aimlworkbench.com/#https://github.com/${{ values.ghOwner }}/${{ values.name }}-research-agent-gitops"
      title: "Open in Dev Spaces"
      icon: catalog
      type: devspaces
  annotations:
    argocd/app-name: ${{ values.name }}-research-agent
    backstage.io/kubernetes-id: ${{ values.name }}
    # Discriminator used by the codex-reauth-ui CodexAuthCard
    # mountPoint filter so the auth-status card only surfaces on
    # agent component pages (not every Component the cluster has).
    agentoffice.ai/agent-kind: research
  tags:
    - ai
    - agent
    - openclaw
    - research
spec:
  type: service
  lifecycle: production
  owner: ${{ values.owner }}
