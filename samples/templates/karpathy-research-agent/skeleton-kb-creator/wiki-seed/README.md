# ${{ values.displayName }} — Knowledge Base

A knowledge base maintained by the `${{ values.name }}` research agent.
The agent watches the topic below, browses the web, and files findings
into this repo. Open this folder as an Obsidian vault to follow along.

**Topic:** ${{ values.topic }}
