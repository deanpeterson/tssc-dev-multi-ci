# ${{ values.displayName }} — Wiki

Karpathy-style autonomous QLoRA research wiki for the
`${{ values.name }}` AutoResearchProject. Maintained by the agent
and the operator. Open this folder as an Obsidian vault to follow
along; the agent-office-operator's KnowledgeBase controller will
auto-populate `GOALS.md`, `DASHBOARD.md`, `agents/`, and the
`log/` + `proposals/` + `results/` folders on first sync.
